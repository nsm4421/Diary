name: app_extensions
description: Extension helpers shared across the Diary app packages.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  path: ^1.9.0
  crypto: ^3.0.3

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: false
